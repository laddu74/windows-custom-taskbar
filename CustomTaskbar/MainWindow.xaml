<Window x:Class="CustomTaskbar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomTaskbar"
        mc:Ignorable="d"
        Title="CustomTaskbar" Height="100" Width="800"
        WindowStyle="SingleBorderWindow" ResizeMode="CanResize"
        AllowsTransparency="False" Background="#1F1F1F"
        Topmost="True" ShowInTaskbar="True" 
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Style for Taskbar Buttons -->
        <Style x:Key="TaskbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" x:Name="border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#11FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" /> <!-- Start Button Area -->
            <ColumnDefinition Width="*" />    <!-- Center/Main Area -->
            <ColumnDefinition Width="Auto" /> <!-- System Tray Area -->
        </Grid.ColumnDefinitions>

        <!-- Glass Effect Border -->
        <Border Grid.ColumnSpan="3" BorderBrush="#33FFFFFF" BorderThickness="0,1,0,0" />

        <!-- Start Button -->
        <Button Grid.Column="0" Width="48" Style="{StaticResource TaskbarButtonStyle}" Margin="4" Click="StartButton_Click">
            <Path Data="M 10 10 L 10 38 L 38 38 L 38 10 Z" Fill="#0078D7" Stretch="Uniform" Margin="8"/>
        </Button>

        <!-- Center Apps Area placeholder -->
        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Explorer" Style="{StaticResource TaskbarButtonStyle}" Height="36" Click="AppButton_Click"/>
            <Button Content="Browser" Style="{StaticResource TaskbarButtonStyle}" Height="36" Click="AppButton_Click"/>
            <Button Content="Terminal" Style="{StaticResource TaskbarButtonStyle}" Height="36" Click="AppButton_Click"/>
        </StackPanel>

        <!-- System Tray Area -->
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
            <TextBlock x:Name="ClockText" Foreground="White" FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                       VerticalAlignment="Center" Margin="10,0" TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
